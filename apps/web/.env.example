# TODO(別PR): 機密値は sops で暗号化管理する (鍵管理方式は別途決定)。
#   このファイルはあくまで「キー名と既定値の見本」であり、実値はコミットしない。
#
# 空のままなら内蔵モックBE (src/lib/mock) で動作します
# 実BE (apps/api など) に接続するときに URL を設定してください
BE_URL=

# 本番 (GCP) で使う想定の変数
# GOOGLE_APPLICATION_CREDENTIALS は Cloud Run 上では不要 (サービスアカウントが自動適用)
